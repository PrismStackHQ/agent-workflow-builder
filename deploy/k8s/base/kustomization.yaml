apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: agent-workflow-system

resources:
  - namespace.yaml
  - postgres.yaml
  - nats-jetstream.yaml
  - rbac.yaml
  - services.yaml
  - ingress.yaml
